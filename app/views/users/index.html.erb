<h1><%= @user.name %>'s Courses</h1>

<% if @user.courses.empty? %>
  <p>You don't have any courses! <%= link_to "Create One", new_user_course_path(@user) %> to start adding courses!</p>

<% else %>
  <% @user.courses.each do |course|%>
    <%= link_to course.title, user_course_path(course) %></br>
  <% end %>

</br> 
</br> 

<%= link_to "Add a New Course", new_user_course_path(@user) %>
<%end%>