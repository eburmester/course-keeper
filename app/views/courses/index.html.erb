<h1><%= current_user.name %>'sCourses</h1>
<% if @user.courses.empty? %>
<p>You don't have any courses! <%= link_to "Create One", new_course_path(current_user) %> to start adding courses!</p>
<% else %>
<% @user.courses.each do |course|%>
  <%= link_to course.title, course_path(course) %>
<% end %>
<%end%>