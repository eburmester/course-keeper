{/* <FORM NAME="newCourse" ACTION="" METHOD="POST">
    Create a new course: <BR>
    <INPUT TYPE="text" NAME="inputbox" VALUE=""><P>
  <INPUT TYPE="button" NAME="button" Value="Click" onClick="createNewCourse(this.form)">
</FORM>

const createNewCourse = () => {
    new Course = form.inputbox.value;
    displayNewCourse(createdCourse);
}

const displayNewCourse = () => {
    let c = createdCourseData => new Course(createdCourseData).render())
} */}